In all instances where you use a USB Ethernet adapter with a FOG Host, you must specify that the NIC is a USB adapter with a kernel parameter on the host’s general page. 

This is the parameter:

<pre>has_usb_nic=1</pre>

Here's what it should look like:

[[file:Has usb nic.png]]